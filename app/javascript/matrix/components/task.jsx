import React from 'react';
// import styled from 'styled-components';

const Task = (props) => {
    return (
        <div>
            <p>{props.name}</p>
        </div>
    );
};

export default Task;
